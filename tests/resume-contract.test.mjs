import assert from "node:assert/strict";
import test from "node:test";
import { validateTailorAnalysis, validateTailorInput } from "../lib/resume-contract.js";
const input = validateTailorInput({ jdText: "岗位需要 Python 分析能力与项目协作经验，负责数据建模和复盘。", resumeText: "候选人\n\n项目经历\n数据分析项目：使用 Python 清洗数据，并完成每周复盘，整理来源、规则与异常记录，形成可复用的团队工作笔记，并在项目结束后交付完整复盘。\n\n教育背景\n大学", userConfirmed: [{ question: "是否带队？", answer: "曾协调两位同事完成访谈。" }] });
test("accepts resume_quote and separately retains user_confirmed evidence", () => { const original = "数据分析项目：使用 Python 清洗数据，并完成每周复盘，整理来源、规则与异常记录，形成可复用的团队工作笔记，并在项目结束后交付完整复盘。"; const result = validateTailorAnalysis(input, { suggestions: [{ title: "数据项目", placement: "前置", action: "原文保留", original, revised: original, sourceEvidence: ["使用 Python 清洗数据"], confirmedEvidence: ["曾协调两位同事完成访谈。"], jdEvidence: "Python 分析能力", reason: "对应建模" }], matches: [{ title: "Python", source: "resume_quote", evidence: "使用 Python 清洗数据", jdEvidence: "Python 分析能力", explanation: "直接证据" }], gaps: [], questions: [], omit: [] }); assert.equal(result.suggestions.length, 1); assert.deepEqual(result.suggestions[0].confirmedEvidence, ["曾协调两位同事完成访谈。"]); });
test("rejects ungrounded numeric and role upgrades", () => { const result = validateTailorAnalysis(input, { suggestions: [{ title: "bad", placement: "前置", action: "重写", original: "数据分析项目：使用 Python 清洗数据，并完成每周复盘。", revised: "主导项目并提升 30% 效率。", sourceEvidence: ["使用 Python 清洗数据"], confirmedEvidence: [], jdEvidence: "Python 分析能力", reason: "bad" }], matches: [], gaps: [], questions: [], omit: [] }); assert.equal(result.suggestions.length, 0); });
